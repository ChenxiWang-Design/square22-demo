# 部署准备清单

## ✅ 已完成的准备工作

1. ✅ 创建了 `api-config.js` - API地址配置文件
2. ✅ 修改了 `register2.js` - 支持使用配置的API地址
3. ✅ 修改了 `server.js` - 更新CORS配置，允许公网访问
4. ✅ 创建了 `railway.json` - Railway部署配置
5. ✅ 创建了 `vercel.json` - Vercel部署配置
6. ✅ 创建了详细的部署步骤文档

---

## 📋 下一步操作

### 步骤1：准备GitHub仓库

1. **如果没有GitHub账号**
   - 访问：https://github.com
   - 注册账号（免费）

2. **创建新仓库**
   - 点击右上角"+" → "New repository"
   - 仓库名称：`square22-demo`（或任意名称）
   - 选择 Public（公开）
   - 点击"Create repository"

3. **推送代码到GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/你的用户名/square22-demo.git
   git push -u origin main
   ```

---

### 步骤2：部署API服务器到Railway

1. **访问Railway**
   - https://railway.app
   - 使用GitHub账号登录

2. **创建新项目**
   - 点击"New Project"
   - 选择"Deploy from GitHub repo"
   - 选择你的仓库

3. **配置项目**
   - Railway会自动检测到项目
   - 在Settings中设置：
     - **Root Directory**: `Register2`
     - **Start Command**: `node server.js`
   - 或者使用我们创建的 `railway.json`（Railway会自动识别）

4. **获取API地址**
   - 部署完成后，在项目页面找到"Generate Domain"
   - 会显示类似：`https://your-api.up.railway.app`
   - **复制这个地址**

---

### 步骤3：修改API配置

1. **修改 `api-config.js`**
   - 找到这一行：
     ```javascript
     return 'https://your-api.up.railway.app'; // TODO: 替换为你的Railway地址
     ```
   - 替换为你的Railway地址：
     ```javascript
     return 'https://your-api.up.railway.app'; // 你的实际Railway地址
     ```

2. **提交更改**
   ```bash
   git add api-config.js
   git commit -m "Update API config for production"
   git push
   ```

---

### 步骤4：部署前端到Vercel

1. **安装Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登录Vercel**
   ```bash
   vercel login
   ```

3. **部署项目**
   ```bash
   vercel
   ```
   - 按提示操作（都选默认即可）

4. **获取前端地址**
   - 部署完成后会显示：`https://your-app.vercel.app`
   - **复制这个地址**

---

### 步骤5：更新CORS配置（如果需要）

如果手机访问API失败，需要更新 `Register2/server.js` 的CORS配置：

```javascript
app.use(cors({
  origin: [
    'https://your-app.vercel.app',  // 你的Vercel地址
    'http://localhost:8080'          // 本地开发
  ],
  credentials: true
}));
```

然后重新部署到Railway。

---

### 步骤6：手机访问

1. **打开手机浏览器**
2. **访问Vercel地址**：`https://your-app.vercel.app`
3. **添加到主屏幕**：
   - iPhone：分享 → 添加到主屏幕
   - Android：菜单 → 添加到主屏幕
4. **像APP一样使用**

---

## ⚠️ 注意事项

1. **API密钥安全**
   - `server.js` 中有硬编码的API密钥
   - 建议在Railway的环境变量中设置 `CLAUDE_API_KEY`
   - 然后修改 `server.js` 使用环境变量

2. **首次部署可能需要时间**
   - Railway部署：约2-3分钟
   - Vercel部署：约1-2分钟

3. **如果遇到问题**
   - 查看Railway日志：项目页面 → "View Logs"
   - 查看Vercel日志：项目页面 → "Deployments" → 点击部署 → "View Function Logs"

---

## 🎯 完成后的效果

- ✅ 手机浏览器访问公网地址
- ✅ 添加到主屏幕，像APP一样使用
- ✅ 全屏显示，没有浏览器地址栏
- ✅ AI功能正常使用
- ✅ 不需要本地服务器
- ✅ 不需要VPN
- ✅ 完全免费

---

## 📞 需要帮助？

如果部署过程中遇到问题，告诉我：
1. 在哪一步卡住了
2. 具体的错误信息
3. 我会帮你解决
