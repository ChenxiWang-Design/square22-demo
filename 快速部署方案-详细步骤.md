# 快速部署方案 - 详细步骤（推荐）

## 🎯 目标
将demo部署到公网，手机浏览器访问并"添加到主屏幕"，像APP一样使用，AI功能正常。

---

## 📋 步骤1：部署API代理服务器（Railway - 免费）

### 1.1 注册Railway账号
- 访问：https://railway.app
- 使用GitHub账号登录（免费）

### 1.2 创建新项目
1. 点击"New Project"
2. 选择"Deploy from GitHub repo"
3. 选择你的项目仓库（如果没有，先创建）

### 1.3 配置项目
1. Railway会自动检测到 `Register2/server.js`
2. 设置环境变量：
   - `PORT=3000`（Railway会自动分配端口，但可以设置）
   - `CLAUDE_API_KEY=你的API密钥`（可选，如果server.js里已有）

### 1.4 部署
- Railway会自动部署
- 等待部署完成，会显示一个URL（例如：`https://your-api.up.railway.app`）

### 1.5 修改server.js（如果需要）
确保server.js监听所有IP：
```javascript
app.listen(PORT, '0.0.0.0', () => {
  // ...
});
```

### 1.6 测试API
访问：`https://your-api.up.railway.app/api/claude`
应该返回404或健康检查响应

---

## 📋 步骤2：修改前端API地址

### 2.1 修改 register2.js

找到这一行（大约715行）：
```javascript
API_URL = 'http://localhost:3000/api/claude';
```

改为：
```javascript
API_URL = 'https://your-api.up.railway.app/api/claude';
```
（替换为步骤1.4获得的Railway URL）

---

## 📋 步骤3：部署前端（Vercel - 免费）

### 3.1 安装Vercel CLI
```bash
npm install -g vercel
```

### 3.2 登录Vercel
```bash
vercel login
```

### 3.3 部署项目
在项目根目录运行：
```bash
vercel
```

按提示操作：
- 是否要部署？输入 `Y`
- 项目名称？直接回车（使用默认）
- 目录？直接回车（使用当前目录）

### 3.4 获得部署地址
部署完成后会显示：
```
✅ Production: https://your-app.vercel.app
```

---

## 📋 步骤4：手机访问

### 4.1 打开手机浏览器
- Chrome、Safari等都可以

### 4.2 访问部署的地址
输入：`https://your-app.vercel.app`

### 4.3 添加到主屏幕
- **Android Chrome**：菜单 → "添加到主屏幕"
- **iOS Safari**：分享按钮 → "添加到主屏幕"

### 4.4 像APP一样使用
- 点击主屏幕图标
- 全屏显示，像原生APP
- AI功能正常使用

---

## ⚠️ 注意事项

1. **API密钥安全**
   - 不要在前端代码中暴露API密钥
   - 使用环境变量或Railway的环境变量配置

2. **CORS配置**
   - 确保Railway的API服务器允许Vercel域名的请求
   - 在server.js中添加：
   ```javascript
   app.use(cors({
     origin: ['https://your-app.vercel.app']
   }));
   ```

3. **VPN问题**
   - Railway服务器在国外，访问Claude API不需要VPN
   - 手机访问也不需要VPN

---

## 🔧 如果遇到问题

### API调用失败
- 检查Railway部署是否成功
- 检查API地址是否正确
- 检查CORS配置

### 前端无法访问
- 检查Vercel部署是否成功
- 检查网络连接

### AI功能不工作
- 检查Railway日志
- 检查API密钥是否正确
- 检查Claude API是否可访问

---

## 📝 总结

**完成后的效果：**
- ✅ 手机浏览器访问公网地址
- ✅ 添加到主屏幕，像APP一样
- ✅ AI功能正常使用
- ✅ 不需要本地服务器
- ✅ 不需要VPN（Railway服务器在国外）

**总耗时：约15-20分钟**
