# 方案2：VPN + ngrok 内网穿透（支持PWA和AI功能）

## 📋 前置准备

### 1. 确保已安装 Node.js
- 如果未安装，访问：https://nodejs.org/
- 安装后，打开命令行输入 `node --version` 验证

### 2. 安装项目依赖

#### 2.1 安装主服务器依赖（项目根目录）
```bash
cd c:\Users\24251\.cursor\Square22
npm install
```

#### 2.2 安装API代理依赖（Register2目录）
```bash
cd c:\Users\24251\.cursor\Square22\Register2
npm install
```

---

## 🔧 安装 ngrok

### 方法1：使用 npm 全局安装（推荐）

1. **打开命令行（PowerShell 或 CMD）**
   - 按 `Win + R`，输入 `cmd` 或 `powershell`，回车

2. **执行安装命令**
   ```bash
   npm install -g ngrok
   ```

3. **验证安装**
   ```bash
   ngrok version
   ```
   如果显示版本号，说明安装成功。

### 方法2：手动下载（如果npm安装失败）

1. **访问 ngrok 官网**
   - https://ngrok.com/download

2. **注册账号（免费）**
   - 访问：https://dashboard.ngrok.com/signup
   - 注册后登录

3. **获取 authtoken**
   - 登录后访问：https://dashboard.ngrok.com/get-started/your-authtoken
   - 复制你的 authtoken（类似：`2abc123def456ghi789jkl012mno345pqr678stu901vwx234yz`）

4. **下载 ngrok**
   - 在下载页面选择 Windows 版本
   - 下载后解压到任意目录（例如：`C:\ngrok`）

5. **配置 authtoken**
   ```bash
   cd C:\ngrok
   ngrok config add-authtoken 你的authtoken
   ```

6. **添加到系统PATH（可选，方便使用）**
   - 右键"此电脑" → 属性 → 高级系统设置 → 环境变量
   - 在"系统变量"中找到 Path，点击编辑
   - 添加 ngrok 所在目录（例如：`C:\ngrok`）

---

## 🚀 启动步骤（按顺序执行）

### 步骤1：启动主服务器（端口8080）

**位置：项目根目录**

1. **打开第一个命令行窗口**
   ```bash
   cd c:\Users\24251\.cursor\Square22
   ```

2. **启动服务器**
   ```bash
   node server.js
   ```

3. **看到以下信息表示成功**
   ```
   ========================================
   主界面预览服务器已启动
   主界面: http://localhost:8080
   ========================================
   ```

4. **保持这个窗口打开**（不要关闭）

---

### 步骤2：启动API代理服务器（端口3000）

**位置：Register2目录**

1. **打开第二个命令行窗口**（新开一个，不要关闭第一个）
   ```bash
   cd c:\Users\24251\.cursor\Square22\Register2
   ```

2. **启动API代理**
   ```bash
   node server.js
   ```

3. **看到以下信息表示成功**
   ```
   ========================================
   代理服务器运行在 http://localhost:3000
   Claude API代理端点: http://localhost:3000/api/claude
   ========================================
   ```

4. **保持这个窗口打开**（不要关闭）

---

### 步骤3：使用 ngrok 生成公网URL

**位置：任意目录（因为ngrok是全局命令）**

1. **打开第三个命令行窗口**（新开一个，不要关闭前两个）

2. **启动 ngrok**
   ```bash
   ngrok http 8080
   ```

3. **看到类似以下信息**
   ```
   Session Status                online
   Account                       your-email@example.com
   Forwarding                    https://abc123.ngrok-free.app -> http://localhost:8080
   Forwarding                    http://abc123.ngrok-free.app -> http://localhost:8080
   ```

4. **复制 Forwarding 中的 URL**
   - 例如：`https://abc123.ngrok-free.app`
   - **这就是你的公网访问地址**

5. **保持这个窗口打开**（不要关闭）

---

## 📱 手机访问

1. **确保电脑连接VPN**（保证AI功能可用）

2. **在手机浏览器访问**
   - 打开手机浏览器（Chrome、Safari等）
   - 输入 ngrok 显示的 URL（例如：`https://abc123.ngrok-free.app`）
   - 首次访问可能需要点击"Visit Site"（ngrok免费版会显示）

3. **测试功能**
   - PWA功能：添加到主屏幕
   - AI功能：在Register2页面测试AI回复

---

## ⚠️ 注意事项

1. **三个窗口都要保持打开**
   - 窗口1：主服务器（端口8080）
   - 窗口2：API代理（端口3000）
   - 窗口3：ngrok（端口8080转发）

2. **ngrok免费版限制**
   - 每次启动URL会变化
   - 有连接数限制
   - 可能需要注册账号

3. **如果ngrok显示"Visit Site"**
   - 这是正常现象（免费版限制）
   - 点击"Visit Site"即可访问

4. **关闭服务**
   - 按 `Ctrl+C` 停止各个服务
   - 先关闭ngrok，再关闭服务器

---

## 🔍 故障排查

### ngrok命令找不到
- 检查是否安装成功：`ngrok version`
- 如果使用手动下载，确保已添加到PATH

### 端口被占用
- 检查8080端口：`netstat -ano | findstr :8080`
- 检查3000端口：`netstat -ano | findstr :3000`
- 关闭占用端口的程序

### ngrok连接失败
- 检查网络连接
- 确认已配置authtoken（如果使用手动下载）
- 尝试重启ngrok

### AI功能不工作
- 确认电脑已连接VPN
- 检查API代理服务器是否正常运行（窗口2）
- 查看API代理服务器的日志输出

---

## 📝 快速命令参考

```bash
# 1. 启动主服务器（窗口1）
cd c:\Users\24251\.cursor\Square22
node server.js

# 2. 启动API代理（窗口2）
cd c:\Users\24251\.cursor\Square22\Register2
node server.js

# 3. 启动ngrok（窗口3）
ngrok http 8080
```
