# 方案二：VPN + 内网穿透（支持PWA和AI功能）

## 📋 准备工作

### 1. 安装 ngrok
```bash
npm install -g ngrok
```

或者访问 https://ngrok.com/download 下载Windows版本

### 2. 注册 ngrok 账号（免费）
访问 https://dashboard.ngrok.com/signup 注册账号，获取 authtoken

### 3. 配置 ngrok
```bash
ngrok config add-authtoken 你的authtoken
```

---

## 🚀 执行步骤（三种方式）

### 方式一：使用自动化脚本（推荐）

1. **双击运行 `方案二-完整启动.bat`**
   - 会自动启动主服务器（8080端口）
   - 会自动启动API代理（3000端口）
   - 会自动启动ngrok（生成公网URL）

2. **等待所有服务启动完成**
   - 主服务器会显示：`主界面预览服务器已启动`
   - API代理会显示：`代理服务器运行在 http://localhost:3000`
   - ngrok会显示公网URL（例如：`https://xxxx.ngrok-free.app`）

3. **在手机浏览器访问ngrok显示的URL**
   - 不需要VPN
   - 可以同时使用PWA和AI功能

---

### 方式二：手动启动（分步执行）

#### 步骤1：启动主服务器（端口8080）

**位置**：项目根目录（`c:\Users\24251\.cursor\Square22\`）

**方法A - 使用批处理文件**：
- 双击运行 `启动主服务器-方案二.bat`

**方法B - 使用命令行**：
```bash
cd c:\Users\24251\.cursor\Square22
node server.js
```

**成功标志**：看到以下信息
```
========================================
主界面预览服务器已启动
主界面: http://localhost:8080
========================================
```

---

#### 步骤2：启动API代理服务器（端口3000）

**位置**：Register2目录（`c:\Users\24251\.cursor\Square22\Register2\`）

**方法A - 使用批处理文件**：
- 双击运行 `启动API代理-方案二.bat`

**方法B - 使用命令行**：
```bash
cd c:\Users\24251\.cursor\Square22\Register2
node server.js
```

**成功标志**：看到以下信息
```
========================================
代理服务器运行在 http://localhost:3000
Claude API代理端点: http://localhost:3000/api/claude
========================================
```

---

#### 步骤3：启动ngrok（生成公网URL）

**位置**：任意目录（ngrok已全局安装）

**方法A - 使用批处理文件**：
- 双击运行 `启动ngrok-方案二.bat`

**方法B - 使用命令行**：
```bash
ngrok http 8080
```

**成功标志**：看到类似以下信息
```
Forwarding   https://xxxx-xxx-xxx-xxx.ngrok-free.app -> http://localhost:8080
```

**重要**：复制显示的 `https://xxxx-xxx-xxx-xxx.ngrok-free.app` 这个URL

---

#### 步骤4：在手机访问

1. 打开手机浏览器（不需要VPN）
2. 访问ngrok显示的URL（例如：`https://xxxx-xxx-xxx-xxx.ngrok-free.app`）
3. 测试PWA功能（添加到主屏幕）
4. 测试AI功能（Register2的AI回复）

---

### 方式三：使用PowerShell脚本（高级）

运行 `方案二-PowerShell启动.ps1`（如果已创建）

---

## 📍 关键位置说明

### 主服务器
- **文件位置**：`c:\Users\24251\.cursor\Square22\server.js`
- **运行目录**：`c:\Users\24251\.cursor\Square22\`
- **端口**：8080
- **作用**：提供主界面和PWA功能

### API代理服务器
- **文件位置**：`c:\Users\24251\.cursor\Square22\Register2\server.js`
- **运行目录**：`c:\Users\24251\.cursor\Square22\Register2\`
- **端口**：3000
- **作用**：代理Claude API请求，解决CORS问题

### ngrok
- **安装位置**：全局安装（通过npm或直接下载）
- **运行位置**：任意目录
- **命令**：`ngrok http 8080`
- **作用**：将本地8080端口映射到公网URL

---

## ⚠️ 注意事项

1. **保持VPN连接**：电脑需要保持VPN连接，以便API代理访问Claude API
2. **三个服务都要运行**：
   - 主服务器（8080）
   - API代理（3000）
   - ngrok（公网URL）
3. **端口占用**：确保8080和3000端口没有被其他程序占用
4. **ngrok免费版限制**：
   - URL每次启动都会变化
   - 有连接数限制
   - 需要注册账号

---

## 🔧 故障排查

### 问题1：主服务器启动失败
- 检查8080端口是否被占用：`netstat -ano | findstr :8080`
- 检查Node.js是否安装：`node --version`
- 检查依赖是否安装：`npm install`

### 问题2：API代理启动失败
- 检查3000端口是否被占用：`netstat -ano | findstr :3000`
- 进入Register2目录安装依赖：`cd Register2 && npm install`

### 问题3：ngrok启动失败
- 检查是否已安装：`ngrok version`
- 检查是否已配置authtoken：`ngrok config check`
- 重新配置authtoken：`ngrok config add-authtoken 你的token`

### 问题4：手机无法访问ngrok URL
- 检查ngrok是否正常运行
- 检查手机网络是否正常
- 尝试在电脑浏览器访问ngrok URL测试

### 问题5：AI功能不工作
- 检查API代理是否正常运行（端口3000）
- 检查VPN是否连接
- 检查Claude API密钥是否有效

---

## 📝 快速参考命令

```bash
# 启动主服务器
cd c:\Users\24251\.cursor\Square22
node server.js

# 启动API代理（新开一个命令行窗口）
cd c:\Users\24251\.cursor\Square22\Register2
node server.js

# 启动ngrok（再新开一个命令行窗口）
ngrok http 8080
```

---

## ✅ 验证清单

- [ ] ngrok已安装并配置authtoken
- [ ] 主服务器已启动（端口8080）
- [ ] API代理已启动（端口3000）
- [ ] ngrok已启动并显示公网URL
- [ ] 电脑保持VPN连接
- [ ] 手机可以访问ngrok URL
- [ ] PWA功能正常（可以添加到主屏幕）
- [ ] AI功能正常（Register2可以回复）
