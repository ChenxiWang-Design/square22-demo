# PWA 使用说明

## 📱 什么是 PWA？

PWA（Progressive Web App）可以让你的网站像原生APP一样使用：
- ✅ 添加到主屏幕
- ✅ 全屏显示（无浏览器地址栏）
- ✅ 离线访问（部分功能）
- ✅ 像原生APP一样的体验

## 🚀 使用步骤

### 1. 生成图标文件（首次使用）

1. 在浏览器中打开 `create-icons.html`
2. 点击"生成图标"按钮
3. 会自动下载两个图标文件：
   - `icon-192.png` (192x192像素)
   - `icon-512.png` (512x512像素)
4. 将这两个文件移动到项目根目录（与 `manifest.json` 同级）

### 2. 启动服务器

```bash
node server.js
```

### 3. 在手机上访问

1. **确保手机和电脑在同一WiFi**（或电脑连接手机热点）
2. 在手机浏览器中访问：`http://你的电脑IP:8080`
3. 浏览器会自动检测PWA功能，并提示"添加到主屏幕"

### 4. 添加到主屏幕

#### Android（Chrome浏览器）：
1. 访问网站后，浏览器会显示"添加到主屏幕"提示
2. 点击提示，或点击浏览器菜单（三个点）→ "添加到主屏幕"
3. 确认添加
4. 主屏幕会出现APP图标

#### iOS（Safari浏览器）：
1. 访问网站后，点击底部分享按钮（方框+箭头）
2. 选择"添加到主屏幕"
3. 确认添加
4. 主屏幕会出现APP图标

### 5. 使用APP

添加后，点击主屏幕的APP图标，就会：
- ✅ 全屏打开（无浏览器地址栏、工具栏）
- ✅ 像原生APP一样使用
- ✅ 支持离线访问（部分功能）

## 🔧 自定义配置

### 修改APP名称和图标

编辑 `manifest.json`：
- `name`: APP完整名称
- `short_name`: APP简短名称（主屏幕显示）
- `description`: APP描述
- `icons`: 图标文件路径

### 更换图标

1. 准备两个PNG图标：
   - 192x192像素
   - 512x512像素
2. 命名为 `icon-192.png` 和 `icon-512.png`
3. 放在项目根目录
4. 重新访问网站即可

## 📝 注意事项

1. **图标文件必须存在**：如果图标文件不存在，PWA功能可能无法正常工作
2. **HTTPS要求**：某些PWA功能（如Service Worker）在HTTPS下才能完全工作，HTTP下部分功能可能受限
3. **浏览器支持**：
   - Android Chrome：完全支持
   - iOS Safari：支持添加到主屏幕，但功能有限
   - 其他浏览器：支持程度不同

## 🐛 故障排除

### 问题：没有出现"添加到主屏幕"提示

**可能原因：**
1. 图标文件不存在 → 检查 `icon-192.png` 和 `icon-512.png` 是否存在
2. manifest.json 路径错误 → 检查浏览器控制台是否有错误
3. 浏览器不支持 → 尝试使用 Chrome（Android）或 Safari（iOS）

**解决方法：**
1. 确保图标文件存在
2. 打开浏览器开发者工具（F12）查看控制台错误
3. 尝试手动添加：浏览器菜单 → "添加到主屏幕"

### 问题：添加到主屏幕后还是显示浏览器

**可能原因：**
- iOS Safari 的限制（iOS Safari 的PWA功能有限）

**解决方法：**
- Android 设备使用 Chrome 浏览器
- iOS 设备可以正常使用，但体验可能不如 Android

## 📞 分享给朋友

### 同一WiFi：
1. 告诉朋友你的电脑IP地址
2. 朋友访问：`http://你的IP:8080`
3. 朋友按照上述步骤添加到主屏幕

### 不同网络：
需要使用内网穿透工具（如 ngrok）：
1. 安装 ngrok：`npm install -g ngrok`
2. 启动服务器：`node server.js`
3. 启动 ngrok：`ngrok http 8080`
4. 获得公网URL（如 `https://abc123.ngrok.io`）
5. 把URL发给朋友
6. 朋友访问URL并添加到主屏幕
